const {
  maxChecks 
} = require('../config')

const {
  requirements,
  validators: {
    isRequired,
    isBoolean
  },
  validator
} = require('../validation')

module.exports = {
  get: () => {

  },
  // Required data: protocol, url, method, successCodes, timeoutSeconds
  // Optional data: none
  post: (data, callback) => {

  },
  put: () => {

  },
  delete: () => {

  }
}